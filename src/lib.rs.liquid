pub mod {{crate_name}};
pub mod {{crate_name}}_impl;

#[cfg(test)]
mod tests {
    use tonic::Request;
    use crate::{{crate_name}}::{{crate_name}}_service_server::{{project-name | pascal_case}}Service;
    use crate::{{crate_name}}::{{project-name | pascal_case}}Request;
    use crate::{{crate_name}}_impl::{{project-name | pascal_case}}ServiceImpl;

    #[tokio::test]
    async fn test_{{crate_name}}() {
        let service  = {{project-name | pascal_case}}ServiceImpl {};
        let request = Request::new({{project-name | pascal_case}}Request {
            name: "Tonic".into(),
        });
        let response =  service.{{rpc_method}}(request).await.unwrap();
        assert_eq!(
            response.into_inner().message,
            "Hello Tonic!".to_string()
        );
    }
}

